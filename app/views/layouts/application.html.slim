doctype html
html
  head
  title RubyCourse
  = stylesheet_link_tag    "application", :media => "all"
  = javascript_include_tag "application"
  = javascript_include_tag "voteable"
  = csrf_meta_tags
  body
    - if current_user
      = link_to('Logout', destroy_user_session_path, :method => :delete)
    p = notice
    p = alert
    = yield
